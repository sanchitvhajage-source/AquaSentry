<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions are all correct -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FloodAlert"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- THIS LINE WAS ADDED TO ENABLE THE FIX -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PermissionsRequestActivity"
            android:exported="false"
            android:label="Permissions" />
        <activity
            android:name=".FloodDashboardActivity"
            android:exported="false"
            android:label="Flood Dashboard" />
        <activity
            android:name=".MapRoutesActivity"
            android:exported="false"
            android:label="Map &amp; Routes" />
        <activity
            android:name=".SafetyTipsActivity"
            android:exported="false"
            android:label="Safety Tips" />
        <activity
            android:name=".EmergencyContactsActivity"
            android:exported="false"
            android:label="Emergency Contacts" />
        <activity
            android:name=".EvacuationRouteActivity"
            android:exported="false"
            android:label="Evacuation Route" />

    </application>

</manifest>